# HandsFree Mobile REAL 0.4 — 생산 준비도 판단모델

## 목적
생산팀장이 장비 한 대를 열었을 때 단순 일정 진행률이 아니라 `실제로 얼마나 끝났는지`, `무엇이 조립을 막는지`, `품질 수정이 재검증됐는지`, `출고 가능한 상태인지`를 근거 중심으로 판단한다.

## 4대 판단축
1. 실제 완성도 vs 계획 소진율
   - `제품마스터 현재 진행률`은 `계획기준`으로만 사용한다.
   - 직접 실적률 근거가 없으면 실제 완성도는 `확인 필요`로 표시한다.
   - 계획값을 실제 완성도로 복사하거나 추정하지 않는다.
   - 90일 분석의 생산여유일·생산상태·병목·실적산출근거를 보조 근거로 사용한다.

2. 핵심자재 준비도
   - 조립 준비도는 자재/외주입고 조건만 판정한다.
   - 품질 재작업은 자재 미입고로 오판하지 않는다.
   - 현재는 열린 자재/외주입고 이슈와 90일 분석의 구매상태·조립가능일을 근거로 사용한다.
   - 부품 단위 핵심자재 원장은 아직 미연결이므로 Part No, 수량, 업체, 실제입고를 생성/추정하지 않는다.

3. 품질 재검증
   - 활성 품질 이슈가 있으면 `재검증 필요`다.
   - `수정했다`는 기록만으로 이슈를 종료하지 않는다.
   - 향후 종료 조건은 증상 → 원인 → 임시조치 → 수정 → 재검증 → 합격 → 종료 순서로 확장한다.

4. 출고 가능 조건
   - 핵심 자재/외주입고, 품질 재검증, 구동·기능시험, 최종검수, 출고승인을 별도 Gate로 본다.
   - 자재 또는 품질에 하드 블로커가 있으면 `출고 조건 보류`다.
   - 검수 완료 + 자재 준비 + 활성 품질이슈 없음은 `출고 가능 후보`일 뿐, 최종 출고승인을 자동 확정하지 않는다.

## V3 읽기 근거
REAL 0.4 read bridge는 기존 6개 범위에 `HF_DATA_90일분석이력!A1:AJ2000`을 추가한다. 여기서 생산여유일, 생산상태, 최종검수, 병목, 구매상태, 조립가능일, 실적산출근거를 읽는다. Google Sheet는 읽기 전용이며 운영 원장을 수정하지 않는다.

## UI
프로젝트 상세 상단에 `생산판단 4대 기준` 패널을 추가한다.
- 실제 완성도
- 계획 소진율 + 생산여유
- 핵심자재 준비도
- 품질 재검증
- 출고 가능 조건
- 조립 가능일 / 생산 여유 / 현재 병목
- 출고 조건 상세 Gate

## 안전규칙
- 실제 완성도 미수집 상태를 숨기지 않는다.
- 계획진행률을 실제 완성도로 대체하지 않는다.
- 부품 단위 원장이 없으면 없는 것으로 표시한다.
- 활성 품질 이슈가 있으면 재검증 Gate를 통과시키지 않는다.
- 출고승인 데이터가 없으면 자동으로 `출고 가능` 확정하지 않는다.
- Write Gate는 계속 잠금 상태다.

## 다음 데이터 확장 우선순위
1. `PART / CRITICAL_MATERIAL` 원장: Project ID, Part No, 품명, 필수수량, 입고수량, 업체, 계획입고, 실제입고, 검사상태, Blocking 여부.
2. `PHYSICAL_PROGRESS` 원장: 프로젝트/공정별 실제 완성률, 잔여공수, 근거 이벤트, 확인자, 확인시각.
3. `QUALITY_VERIFICATION` 원장: 이슈 ID, 수정조치, 재검증 방법, 결과, 확인자, 완료시각.
4. `SHIPMENT_GATE` 원장: 조립, 전장, 프로그램, 시험, 안전인터록, 검수/FAT, 수정종료, 출고승인 상태.
